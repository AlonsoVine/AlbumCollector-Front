
const BACK_IP: string = "localhost";

const BACK_PORT: number = 8082;

const API_URL: string = "api"

///////

export const BACK_URL = 'http://' + BACK_IP
               + ':' + BACK_PORT + '/'
               + API_URL;
